{
    "title": "Phase",
    "type": "object",
    "properties": {
        "contestId": {
            "type": "integer",
			"minLength": 1
        },
        "name": {
            "type": "string",
			"minLength": 1
        },
		"groupPhase": {
            "type": "boolean"
        }
    },
    "required": ["name", "contestId", "groupPhase"]
}